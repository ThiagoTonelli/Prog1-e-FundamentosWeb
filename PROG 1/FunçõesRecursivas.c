// Faça um programa que some dois numeros e se algum dos numeros 
// for negativo converta ele em positivo.


/*#include <stdio.h>

int main () {
float a, b, somafinal;	
	
	float soma_num(float num1, float num2);
	float conversor(float x);
	
	printf("Digite 2 numeros: ");
    scanf("%f", &a);
    scanf("%f", &b);
    
    printf("\nA soma final eh: %f", soma_num(a, b));
}



float soma_num (float num1, float num2) {
	
	float conversor(float x);
	
	if (num1 < 0) {
		num1 = conversor(num1);
	}
	if (num2 < 0) {
		num2 = conversor(num2);
    }
	return num1 + num2;
	
}

float conversor(float x) {
	
	return x * -1;
}*/

//Faça um programa que calcule o fatorial e use recursividade

/*#include <stdio.h>

int main () {
int fatorial(int x);
int num1, resultado;	
	printf("Digite o numero: ");
	scanf("%d", &num1);
    resultado = fatorial(num1);
	printf("\nO resultado eh: %d.", resultado);
}

int fatorial (int x) {
int resultado;	
	
	if (x == 0) {
		resultado = 1;
	}
	else { 
	    resultado = x * fatorial(x - 1);
	}
	

return resultado;
} */
